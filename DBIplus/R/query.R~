dbGetQuery <- function(conn, statement, ..., .as_tibble = TRUE) {
    RV <- DBI::dbGetQuery(conn, statement, ...)

    if(is.data.frame(RV) &&
       as_tibble &&
       requireNamespace("tibble", quietly = TRUE)) {
        RV <- tibble::as_tibble(RV)
    }

    return(RV)
}
